from tkinter import OFF


class Superhero: 
    def __init__(self,superpower,strength,physical_characteristic):
        self.superpower= ""
        self.strength= ""
        self.physical_characteristic=""
thor_nour = Superhero("fly","strong","short")


def attack(self, enemy):
    game_is_on
   
    if self.is_standing and enemy.is_standing: 
        FLYING = random.randint(1,FLYING_LIMIT)         
        enemy.stands = enemy.stands[:len(enemy.stands) - FLYING] 
        
        if flying >= HAMMER_BLOW:
          
          print(f"Hammer Blow to {enemy.name} ! damage: {damage} \n")

          screams = random.choice(enemy.screams)
          print(f"{enemy.name} says: {screams}")
            
          
          print(f"{enemy.name}  Blood loss left:")
          blood lost left = ""
          for blood lost in enemy.stands:
            
            
          print(blood loss left arm + str(len(enemy.stands)) + "\n")


            else:
          print(f'No points by {self.name} \n')

        if len(enemy.stands) <= 2:
          
          enemy.is_standing=False
          game_is_on=False
          print(f" {enemy.name} has been killed!!! ")
          print("GAME OVER")
          
        if len(self.stands) <= 2:
          self.is_standing = False
          game_is_on = False
          print(f" {self.name} has been slain!!!")
          print(" GAME OVER ")

          def fill_health(self):
    
      amt = random.randint(LIVES_BOTTOM_RANGE, LIVES_TOP_RANGE)
        
      for i in range(0, amt):
          self.stands.append("")

          print ("your new stats are")

          game is off